/*
#include<iostream>
#include<bitset>

using namespace std;

int main()
{
	int a, b = { 0 };
	cout << "A is ? , ? = ";
	cin >> a;
	cout << "B is ? , ? = ";
	cin >> b;

	if (a > b)
	{
		cout << "A가 크다." << endl;
	}
	else
	{
		cout << "B가 크다." << endl;
	}

	if (a > b) 
	{   //a와 b의 값을 바꾸는 공식
		int temp = a;
		a = b;
		b = temp;
		cout << "a와 b의 값을 바꾸겠다! "<<" a는 " << a << " b는 " << b << endl;
	}
	else
	{
		cout <<"그대로! " << " a는 " << a << " b는 " << b << endl;
	}
}
*/